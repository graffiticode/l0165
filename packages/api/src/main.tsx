import React from 'react';
import ReactDOM from 'react-dom/client';
import { View } from '../../web/dist/index.es.js';
import '../../web/dist/style.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <View />
  </React.StrictMode>,
)
