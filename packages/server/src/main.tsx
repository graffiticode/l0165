import React from 'react';
import ReactDOM from 'react-dom/client';
import { View } from '../../client/dist/index.es.js';
import '../../client/dist/style.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <View />
  </React.StrictMode>,
)
